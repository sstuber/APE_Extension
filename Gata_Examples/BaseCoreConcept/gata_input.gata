reify(
  pi1(
    sigmale(
      bowtie(
        revert(contour_noise),
        deify(
          merge(
            pi2(sigmae(objectregions_muni, object_Utrecht))
          )
        )
      )
    ,
      70
    )
  )
)

